string opc = "";
List<string> opcoes = new List<string>();
opcoes.Add("1 - Login");
opcoes.Add("2 - Sair");

while (true)
{
    switch (opc)
    {
        case "1":
            break;
        case "2":
            break;
    }
    if (opc == "2")
    {
        return;
    }
}